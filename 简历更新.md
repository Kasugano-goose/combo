Java基础扎实，熟悉集合、多线程、反射、注解；理解JVM内存模型及垃圾回收机制；

熟练使用Spring Boot、Spring Data JPA、Websocket(STOMP/JSR 356) 、能独立设计分层架构；

熟悉tomcat等常见应用服务器；

熟悉MySQL、SQL Server等主流数据库管理系统、相关的技术及工具，使用Redis实现匹配池、分布式锁、Lua脚本原子操作、缓存及超时管理(TTL)；

熟悉常见网络协议，了解Netty网络编程模型及EventLoop机制，了解操作系统进程与线程模型；

熟练使用Maven、Postman、idea、Git等项目开发及管理工具，并能结合AI工具提升编码与调试效率；了解Linux基础命令及Nginx简单配置；

项目经历
combo实时对战平台
后端核心开发者
2026.03 - 至今
项目简介： 一款为面向校园电竞社的轻量化实时对战平台。系统涵盖玩家匹配、好友社交、实时聊天、场景同步等完整游戏后端闭环。通过引入两阶段确认机制与服务端权威广播，保障高并发匹配场景下的数据一致性及实时对战中的低延迟同步。

技术栈： Spring Boot、Spring Data JPA、MySQL、Redis（分布式锁、Lua脚本）、WebSocket(JSR 356)。

技术亮点：

匹配系统高并发竞态控制与分布式改造： 匹配池在多人同时匹配时，可能出现同一玩家被重复配对或匹配失败的问题。设计两阶段确认机制——匹配成功后不立即创建场景，而是等待双方客户端各自发送确认指令，待双方就绪后再由 SceneService 统一创建场景和游戏循环。针对分布式部署需求，将匹配对存储从本地 ConcurrentHashMap 迁移到 Redis（Hash + TTL 30s），使用 Lua 脚本保证匹配确认、匹配移除的原子性；基于 Redis SETNX 实现分布式锁（玩家级粒度，5s 自动过期防死锁），替代 synchronized 关键字，支持多实例水平扩展。该方案彻底规避了 WebSocket 会话未注册导致的场景进入失败，本地模拟20人同时匹配，匹配成功率约98%（相比无确认机制提升约13%）。

实时对战的低延迟位置同步：传统 HTTP 轮询无法满足实时对战要求。采用服务端权威架构，服务端以 100ms 为周期定时广播游戏状态（玩家坐标、移动方向），客户端仅负责 Canvas 渲染及键盘输入采集。相比客户端同步方案，此设计从根本上杜绝了位置篡改的可能，并将多端状态一致性偏差控制在 100ms 以内。

好友关系双向一致性保障： 好友申请通过后，需要在 friendship 表中插入两条互为反向的记录（A→B 和 B→A）。通过 @Transactional 事务包装双向插入操作，保证任何一条插入失败时整体回滚，避免了单向好友的数据不一致问题。

WebSocket 多端点会话管理： 原有 WebSocketSessionManager 仅支持单会话存储，Chat 和 Friend 端点会互相覆盖同一玩家的 Session。重构为 ConcurrentHashMap<Long, Map<String, Session>> 结构，支持同一玩家同时维护多个 WebSocket 连接（chat、friend、scene），确保各类通知（匹配、好友、聊天）互不干扰。

校园技术论坛
后端开发
2025.12 - 2026.01
项目简介：一个面向校内技术爱好者的轻量级论坛，支持用户注册登录、发布帖子、评论回复、按版块浏览、关键词搜索及简单管理功能。

技术栈：Spring Boot + MyBatis-Plus + MySQL + Redis + Thymeleaf

主要工作：

参与用户认证与权限控制模块，使用 Spring Session + 拦截器实现登录态管理，区分普通用户和管理员角色。

实现帖子与评论的 CRUD 操作，使用 MyBatis-Plus 的 Page 分页插件实现帖子列表懒加载。

利用 MyBatis-Plus 条件构造器实现标题/内容的模糊搜索。

配合 Redis 缓存首页热帖（TTL 5 分钟），手动实现"先查缓存，未命中查数据库"的逻辑，并在发帖/点赞后失效缓存。

自我评价
具备完整项目落地经验的后端开发者，擅长解决高并发场景下的数据一致性问题，熟悉分布式锁、Lua脚本原子操作等分布式一致性方案。

学习自驱力强，主动攻克技术难点，并能总结为可复用的方案。

乐于参与团队协作，善于倾听和沟通，能快速融入开发流程，有责任心。

保持对开源社区的持续关注，关注游戏后端、高并发相关的开源项目、善于吸收优秀实践并转化为个人项目经验。
